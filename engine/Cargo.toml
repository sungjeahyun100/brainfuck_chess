[package]
name = "brainfuck-chess-engine"
version = "0.1.0"
edition = "2021"

[features]
profiling = []

[dependencies]
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"

[dev-dependencies]
