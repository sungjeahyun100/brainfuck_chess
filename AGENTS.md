# AGENTS.md

## 1. 문서 목적

이 문서는 이 저장소에서 작업하는 AI 에이전트가 따라야 할 개발 원칙, 변경 절차, 안전 경계와 검증 기준을 정의한다.

에이전트는 작업을 시작하기 전에 관련 코드, 테스트, 설정 파일과 문서를 먼저 확인해야 한다. 저장소에서 확인되지 않은 구조나 요구사항을 임의로 가정하지 않는다.

---

## 2. 지침 우선순위

충돌하는 지침이 있을 경우 다음 우선순위를 따른다.

1. 사용자의 명시적인 요구사항
2. 기존 동작과 하위호환성 보존
3. 보안, 데이터 무결성 및 안전 경계
4. 테스트와 검증 가능성
5. 최소하고 국소적인 변경
6. 이 문서의 일반적인 설계 원칙

사용자가 요구하지 않은 기능, 리팩터링, 프레임워크, 추상화 계층 또는 다중화 시스템을 임의로 추가하지 않는다.

---

## 3. 프로젝트 및 호환성 원칙

### Chessembly 하위호환성

기존 Chessembly 문법과 동작의 하위호환성은 핵심 요구사항이다.

* 원본 Chessembly 환경에서 동작하는 코드는 특별한 사유가 없는 한 이 엔진에서도 동일하게 동작해야 한다.
* 기존 문법의 의미를 조용히 변경하지 않는다.
* 새로운 문법이 기존 문법을 가리거나 모호하게 재해석하게 만들지 않는다.
* 기존 문법과 확장 문법의 경계를 명확히 유지한다.
* 호환성에 영향을 주는 변경에는 회귀 테스트를 추가한다.
* 의도적인 호환성 파괴가 필요한 경우 구현 전에 그 영향과 대안을 명확히 알린다.

### 기존 동작 보존

* 사용자가 명시적으로 요구하지 않은 기존 기능을 삭제하지 않는다.
* 사용되지 않는 것처럼 보인다는 이유만으로 레거시 동작을 제거하지 않는다.
* 공개 API, 저장 형식, 설정 키와 문법의 의미를 임의로 변경하지 않는다.
* 기존 동작이 불명확하면 구현 전에 코드와 테스트를 통해 실제 동작을 확인한다.

---

## 4. 작업 범위 원칙

### 최소 변경

* 요청을 해결하는 데 필요한 범위만 수정한다.
* 관련 없는 리팩터링을 함께 수행하지 않는다.
* 기존 구조로 해결할 수 있으면 새로운 프레임워크나 추상화 계층을 만들지 않는다.
* 기존 추상화를 확장하는 것과 새로운 추상화를 만드는 것 중 더 단순하고 명확한 방법을 선택한다.
* 변경 파일 수가 많아질 경우 각 파일을 수정해야 하는 이유를 설명한다.

### 과설계 금지

이 문서의 설계 원칙은 기능의 위험도와 복잡도에 비례하여 적용한다.

보안 경계, 외부 입력, 권한, 저장, export, 복구와 같은 영역에는 엄격하게 적용한다.

단순한 로컬 함수, UI 표현 변경, 테스트 코드 또는 간단한 데이터 변환에는 원칙을 형식적으로 충족하기 위한 불필요한 객체, registry, fallback, voter 또는 파일 분리를 만들지 않는다.

---

## 5. 사용자 통제권

### 유저에게 맡겨라

* 안전한 기본값을 제공한다.
* 위험하거나 권한이 필요한 기능은 명시적인 opt-in 뒤에 활성화한다.
* 사용자가 선택할 수 있는 항목은 가능한 경우 명확하게 제시한다.
* 선택지가 존재한다는 이유만으로 신뢰 경계, 권한 검사 또는 안전 정책을 우회하지 않는다.
* 되돌리기 어려운 작업은 실행 전에 영향과 복구 가능 여부를 알린다.
* 자동화는 사용자가 허용한 범위 안에서만 수행한다.

### 유저에게 알려라

사용자에게 필요한 상태를 내부 비밀을 노출하지 않는 범위에서 명확하게 알린다.

필요한 경우 다음 상태를 구분하여 전달한다.

* 현재 primary 경로의 상태
* 격리 여부
* degraded 또는 fallback 경로 사용 여부
* export 차단 여부
* Last Known Good 사용 여부
* 읽기 전용 상태 여부
* confirmed fix가 보류된 이유
* 자동 복구가 진행 중인지 여부
* 사용자 개입이 필요한 최소 행동

내부 secret, 원문 민감 데이터 또는 보안상 공개할 수 없는 구현 세부사항은 노출하지 않는다.

---

## 6. 입력 및 데이터 경계

### 사용자의 입력을 신뢰하지 않는다

모든 외부 입력은 처음에는 `unknown` 또는 `untrusted` 상태로 취급한다.

입력은 다음 단계를 통과한 뒤에만 내부 normalized object로 승격한다.

1. 구조 확인
2. 타입 확인
3. 범위 검사
4. 형식 정규화
5. 권한 및 정책 확인
6. 필요한 경우 redaction
7. 내부 불변조건 검사

추가 원칙:

* 입력 검증과 비즈니스 로직을 분리한다.
* 파싱 성공을 의미 검증 성공으로 간주하지 않는다.
* optional 값이 없을 경우 `undefined` 같은 의미 없는 값을 저장하지 않고 필드를 생략한다.
* 누락, 빈 값, 명시적인 `null`, 기본값 적용 상태를 가능한 한 구분한다.
* 정규화 전 원문 입력을 여러 계층에 무분별하게 전달하지 않는다.
* 민감한 원문 입력은 필요한 최소 계층만 접근할 수 있게 한다.
* redaction 실패 시 원문을 그대로 다음 단계에 전달하지 않는다.

---

## 7. 설정과 정책 관리

### 하드코딩을 지양한다

변경 가능성이 있거나 정책적 의미가 있는 정적 값은 책임별 registry, 설정 또는 policy 파일에서 관리한다.

다만 단일 위치에서만 사용되는 명백하고 안정적인 상수를 무조건 별도 registry로 분리하지 않는다.

다음 종류의 값은 하나의 거대한 registry에 섞지 않는다.

* 사용자에게 표시되는 문구
* 고정 용어와 도메인 명칭
* safety grade
* quorum policy
* retry policy
* error code
* 권한 정책
* export 정책
* provider 설정

추가 원칙:

* 정책 값과 사용자 문구를 분리한다.
* 보안 기본값은 런타임 입력만으로 약화시킬 수 없게 한다.
* 설정의 출처와 우선순위를 명확히 한다.
* 잘못된 설정은 조용히 무시하지 않고 명시적인 오류 또는 안전한 기본값으로 처리한다.
* 설정 파일 변경에는 가능한 경우 스키마 검증을 적용한다.

---

## 8. 관심사와 책임 분리

### SoC와 SRP를 따른다

책임과 변경 이유가 명확히 다른 요소는 분리한다.

다음 항목이 서로 다른 이유로 변경된다면 분리를 고려한다.

* 입력 파싱
* 의미 검증
* 도메인 규칙
* 상태 전이
* 저장
* 외부 통신
* 사용자 표현
* 오류 변환
* 정책 판단
* 관측과 감사

단, 단순한 기능을 형식적으로 여러 파일이나 객체로 쪼개지 않는다.

분리로 인해 인터페이스 수, 상태 전달, 테스트 비용과 이해 비용이 더 커진다면 현재 구조를 유지할 수 있다.

“100% 분리”를 목표로 하기보다 다음을 목표로 한다.

* 책임이 설명 가능할 것
* 변경 이유가 명확할 것
* 의존 방향이 예측 가능할 것
* 테스트 경계가 명확할 것
* 한 구성요소가 불필요하게 많은 정보를 알지 않을 것

---

## 9. 계약과 객체화

### 시스템의 책임과 경계를 명시한다

“모든 시스템을 객체화한다”는 것은 class 수를 늘리라는 뜻이 아니다.

복잡하거나 보안에 민감한 기능은 다음 항목을 명시적인 계약으로 표현한다.

* responsibility
* boundary
* input
* output
* failure
* evidence
* recovery action
* safety decision

계약은 상황에 따라 다음 형태 중 가장 단순한 것으로 구현할 수 있다.

* 타입
* 인터페이스
* 구조체
* 함수 계약
* 스키마
* 명시적인 결과 객체
* 상태 머신
* 정책 객체

단순한 로컬 로직에는 불필요한 객체 계층이나 wrapper를 만들지 않는다.

boolean 하나로 성공, 실패, degraded, blocked와 같은 여러 상태를 표현하지 않는다. 상태의 의미가 여러 개라면 명시적인 enum 또는 결과 타입을 사용한다.

---

## 10. 다중화와 독립성

### 필요한 경우 독립적인 보조 경로를 둔다

단일 실패가 안전성, 데이터 무결성 또는 핵심 기능 지속성에 영향을 주는 경우에만 보조 경로를 설계한다.

가능한 보조 경로의 예:

* conservative lane
* fallback implementation
* shadow audit
* 독립 voter
* 독립 monitor
* read-only Last Known Good
* secondary provider

다중화는 구현 수를 늘리는 것만으로 달성되지 않는다.

다음은 독립적인 다중화로 간주하지 않는다.

* 동일 parser 결과를 공유하는 두 검증기
* 동일 HTTP mapper를 사용하는 두 provider adapter
* 동일 pattern list를 사용하는 두 safety filter
* 동일 failure mode를 공유하는 복제 구현
* 이름만 다른 동일한 판단 순서

보조 시스템은 가능한 경우 다음 중 일부가 달라야 한다.

* 구현
* 입력 source
* 판단 순서
* 의존성
* failure mode
* 권한 범위
* 관측 정보

작은 기능이나 안전성과 무관한 기능에 다중화 구조를 강제로 추가하지 않는다.

---

## 11. Fail-Operational

### 고장 중에도 안전 기준을 낮추지 않는다

고장이 발생해도 confirmed 판정 기준이나 안전 기준을 낮추지 않는다.

가능한 경우 다음 방식으로 기능 연속성을 유지한다.

* metadata-only degraded output
* 안전한 fallback
* read-only Last Known Good
* 제한된 기능 모드
* export 차단 상태에서의 내부 조회
* 격리된 lane의 제외
* 보수적인 결과 제공

다음 외부 시스템을 애플리케이션이 몰래 수정하거나 재시작하지 않는다.

* 외부 provider
* Docker daemon
* GPU driver
* 운영체제 서비스
* 사용자 소유 worker process
* 외부 데이터베이스
* 사용자 설정

외부 시스템 조작이 필요한 경우 상태와 필요한 행동을 사용자에게 알린다.

---

## 12. 오류 처리와 인간 개입

### 오류 시 인간 개입을 최소화한다

오류가 발생하면 먼저 내부에서 가능한 안전한 처리를 수행한다.

우선순위:

1. 오류 상태 기록
2. 영향 범위 격리
3. 안전 경계 적용
4. idempotency 확인
5. 허용된 bounded retry
6. 안전한 backup 또는 fallback 선택
7. 필요 시 export 차단
8. 사용자에게 최소 행동 요청

사람에게는 다음과 같이 자동으로 해결할 수 없는 마지막 행동만 요청한다.

* 자동 재수집 승인
* 파일 선택
* 외부 provider 상태 확인
* 권한 부여
* 위험 기능 opt-in
* 충돌 해결을 위한 도메인 판단

사용자에게 내부 로그 전체를 해석하도록 떠넘기지 않는다.

---

## 13. 재시도 정책

### 재시도 불가능한 오류

다음 오류는 원칙적으로 자동 재시도하지 않는다.

* 구조적으로 잘못된 입력
* 의미적으로 유효하지 않은 입력
* redaction 실패
* unsafe output
* 정책 차단
* 권한 차단
* quorum conflict
* 결정적인 파싱 오류
* 지원하지 않는 형식
* 사용자 확인이 필요한 충돌

이 경우 다음 사항을 명시한다.

* retry 금지 사유
* fallback 가능 여부
* backup 또는 LKG 사용 여부
* degraded 전환 여부
* fail-safe 전환 기준
* 필요한 사용자 행동

### 재시도 가능한 일시적 오류

재시도 가능한 오류는 idempotency를 확인한 뒤 bounded retry를 수행한다.

재시도 정책에는 가능한 경우 다음 요소를 포함한다.

* 최대 시도 횟수
* 각 시도의 timeout
* backoff
* jitter
* 전체 retry budget
* abort 조건
* circuit breaker
* audit 기록

“즉시 고친다”는 무한 재시도를 의미하지 않는다.

내부 reset 이후에는 redacted health audit을 통과한 lane만 정상 경로 또는 voting에 다시 참여시킨다.

---

## 14. Fail-Safe

### Fail-Operational이 불가능하면 안전하게 정지한다

고장을 복구할 수 없고 안전한 backup도 사용할 수 없다면 Fail-Safe로 전환한다.

Fail-Safe 상태에서는 다음을 우선한다.

* unsafe output 차단
* 손상 가능성이 있는 쓰기 차단
* export 차단
* 외부 전송 차단
* 읽기 전용 상태 전환
* 확인되지 않은 결과 제공 금지
* 안전한 진단 정보 제공

조용히 잘못된 결과를 반환하는 것보다 명시적으로 중단하는 것을 우선한다.

---

## 15. 심층 방어

### 계층마다 다른 정보와 권한을 갖게 한다

심층 방어는 동일 검사를 여러 번 반복하는 것이 아니다.

각 계층은 필요한 최소 정보와 권한만 가져야 한다.

가능한 경우 다음과 같이 경계를 분리한다.

* 원문 입력 접근 계층
* 정규화 계층
* redaction 계층
* 정책 판정 계층
* 실행 계층
* export 계층
* 감사 계층

한 계층 또는 두 계층이 침해되더라도 다음을 재구성하거나 획득하기 어렵게 설계한다.

* 원문 민감 데이터
* secret
* 권한 토큰
* unsafe output
* 전체 사용자 상태

추가 원칙:

* 방어 계층은 가능한 경우 정규화된 입력만 받는다.
* redaction 이전 원문을 여러 계층에 복제하지 않는다.
* redaction 실패 시 다음 계층으로 진행하지 않는다.
* degraded, fallback 또는 fail-safe 경로는 정책으로 결정한다.
* 로그와 감사 데이터에도 redaction을 적용한다.
* 테스트용 우회 경로가 운영 환경에서 활성화되지 않게 한다.

---

## 16. 의존성과 외부 도구

* 새로운 의존성을 추가하기 전에 기존 의존성으로 해결할 수 있는지 확인한다.
* 의존성을 추가할 경우 필요성, 대안, 유지보수 위험을 설명한다.
* lock 파일과 버전 정책을 존중한다.
* 생성된 파일, 빌드 결과물과 외부 vendor 코드는 직접 수정하지 않는다.
* 외부 API 또는 provider의 동작을 추측하지 말고 현재 adapter, 테스트와 문서를 확인한다.
* 외부 명령 실행은 현재 권한 범위 안에서 수행한다.
* 저장소 밖의 파일이나 사용자 환경을 변경하기 전에 명시적인 승인을 받는다.

---

## 17. 구현 절차

코드를 수정하기 전에 다음을 수행한다.

1. 관련 파일을 찾는다.
2. 현재 동작을 확인한다.
3. 관련 테스트를 찾는다.
4. 변경의 영향 범위를 파악한다.
5. 하위호환성 영향을 확인한다.
6. 가장 작은 구현 경로를 선택한다.

작업 중에는 다음을 지킨다.

* 관련 없는 코드를 수정하지 않는다.
* 임시 우회 코드를 최종 구현으로 남기지 않는다.
* 오류를 빈 catch 또는 무시 처리로 숨기지 않는다.
* 타입 오류를 강제 캐스팅만으로 우회하지 않는다.
* 테스트를 통과시키기 위해 요구사항을 약화시키지 않는다.
* 보안 검사를 제거하거나 비활성화하지 않는다.
* 실패 상태를 성공 상태로 변환하지 않는다.

---

## 18. 테스트 및 검증

변경 후 가능한 범위에서 다음을 수행한다.

1. 관련 단위 테스트 실행
2. 관련 통합 테스트 실행
3. 새로운 동작에 대한 테스트 추가
4. 하위호환성 회귀 테스트 추가
5. 빌드 실행
6. 타입 검사 실행
7. lint 또는 정적 분석 실행
8. `git diff` 검토
9. 의도하지 않은 파일 변경 확인

저장소의 정확한 명령을 알 수 없다면 다음 파일을 먼저 확인한다.

* `README.md`
* `package.json`
* `pom.xml`
* `build.gradle`
* `CMakeLists.txt`
* `Makefile`
* CI workflow
* 기존 개발 문서

검증 명령을 찾지 못한 경우 임의의 명령을 만들어내지 말고, 어떤 검증을 수행하지 못했는지 명시한다.

테스트가 실패한 경우 테스트를 삭제하거나 약화시키기 전에 구현이 잘못되었는지 먼저 확인한다.

---

## 19. 작업 완료 보고

작업 완료 시 다음을 간단히 보고한다.

* 무엇을 변경했는지
* 왜 해당 방식으로 변경했는지
* 변경한 주요 파일
* 실행한 테스트와 검증 명령
* 테스트 결과
* 남아 있는 위험이나 불확실성
* 수행하지 못한 검증
* 하위호환성에 미치는 영향

실제로 실행하지 않은 테스트를 실행했다고 말하지 않는다.

확인하지 않은 동작을 정상이라고 단정하지 않는다.

---

## 20. 금지 사항

명시적인 요청이나 충분한 근거 없이 다음 행동을 하지 않는다.

* 관련 없는 대규모 리팩터링
* 기존 API 또는 문법 제거
* 하위호환성 파괴
* 보안 기본값 약화
* 권한 검사 우회
* 테스트 삭제 또는 무력화
* 비밀 정보 로그 출력
* 사용자 입력을 검증 없이 실행
* 외부 시스템 자동 재시작
* 저장소 밖 파일 변경
* 새로운 대형 프레임워크 도입
* 동일 구현을 복제한 가짜 다중화
* 사소한 기능을 위한 과도한 객체화
* 실패를 성공으로 위장하는 fallback
* 사용자의 명시적 동의 없는 위험 기능 활성화
