# Deck Chess Bot Search Upgrade — 구현 지시문

기준 저장소: `sungjeahyun100/brainfuck_chess`
기준 브랜치: `develop`

중요 원칙:

* 작업 시작 전에 반드시 현재 `develop` 브랜치의 실제 코드를 다시 읽고 아래 지시와 코드가 충돌하면 현재 구조를 우선 분석한다.
* 기존 게임 규칙, Chessembly 동작, 커스텀 기물, 특수능력, 포켓 시스템의 의미를 변경하지 않는다.
* 서버/클라이언트에서 제출되는 외부 action에 대한 canonical validation은 약화하지 않는다.
* AI 검색 내부 최적화를 위해 별도 trusted/internal 경로를 만드는 것은 허용한다.
* 기존 테스트를 깨뜨리지 않는다.
* 각 단계는 독립적으로 테스트 가능해야 한다.
* 성능 최적화 때문에 검색 결과의 정확성을 희생하지 않는다.
* 아직 구현하지 않은 이후 단계의 기능을 미리 대규모로 끌어오지 않는다.
